version: '3.8'
services:
  swap-processor:
    build: .
    container_name: swap-processor
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: default
      JAVA_OPTS: "-Xms256m -Xmx512m"
    restart: unless-stopped
